# Belief Observatory

Auditable belief graph — the site **is** the product story for ACME / MemoryBench.

- **Static**: `static/index.html`, `css/observatory.css`, `js/app.js`, `js/api.js`, `js/trace-fallback.json`
- **API**: `GET /api/trace` — canonical CRS trace steps from `api/belief_data.py`
